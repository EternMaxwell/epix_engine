# Bevy Mesh, Core Pipeline, Sprite, and Image — Epix Match Status

Reference: Bevy tag `v0.18.0` (commit
`5f8270f2e049f90139a503d1e930070d926f9427`) in `tmp/bevy-0.18.0`, compared
with `epix_engine/mesh`, `epix_engine/render/core_graph`,
`epix_engine/sprite`, `epix_engine/sprite_render`, and `epix_engine/image` (C++23 no-modules
build). The sprite audit covers both
Bevy's `bevy_sprite` and `bevy_sprite_render` crates.

This is the source-of-truth tracker for the mesh/core-graph parity effort.
Every row is retained until it is fixed or the user explicitly accepts it as
intentional, deferred, or out of scope.

## Legend

| Status | Meaning |
| --- | --- |
| ✅ | Functionally equivalent |
| ⚠️ | Intentional divergence; rationale recorded |
| ❌ | Present but behavior or API differs; must fix |
| 🚫 | Missing; must implement |
| REVIEW | Audit incomplete; not yet a parity claim |
| N/A | Non-implementable or out of scope, only with user confirmation |

## Translation Rules

| Bevy | Epix |
| --- | --- |
| `Option<T>` | `std::optional<T>` / optional reference wrapper for borrowing |
| `Result<T, E>` | `std::expected<T, E>` |
| ECS resource and system | Epix resource and scheduled system |
| Rust iterator/slice | C++ range/`std::span` view |
| `wgpu` resource wrapper | Direct `wgpu::` resource where that is an accepted Epix convention |

## Mesh Render Asset and Allocator

| ID | Bevy entity / behavior | Epix counterpart | Status | Notes |
| --- | --- | --- | --- | --- |
| M1 | `MeshRenderAssetPlugin`: registers render asset + allocator and initializes vertex layouts | `render::mesh::MeshRenderAssetPlugin` | ✅ | The render-owned plugin composes `RenderAssetPlugin<Mesh>` and `MeshAllocatorPlugin`, then initializes render-world `MeshVertexBufferLayouts`. Mesh2d rendering consumes it from the separate sprite-render module instead of owning allocator state. |
| M2 | `RenderMesh`: CPU render metadata, vertex-layout reference, indexed buffer metadata | `RenderMesh` (replaces `GPUMesh`) | ✅ | `GPUMesh` is removed; `RenderMesh` (`vertex_count`, `RenderMeshBufferInfo`, `MeshVertexBufferLayoutRef`, primitive topology) is the `RenderAsset<Mesh>::ProcessedAsset`. Mesh2d draw commands bind the vertex/index buffers from `MeshAllocator::mesh_vertex_slice`/`mesh_index_slice` with the interleaved layout's stride/offsets, and the pipeline cache specializes per interned layout (Bevy Mesh2dPipelineKey's layout id). Verified: 27 mesh + 189 render + text tests, plus client-area captures of mesh_rendering (1280x720, center (174,195,189) — identical to the pre-rework baseline), alpha_mask ((253,160,79)), batching ((42,42,46)), and font_image — all unchanged. |
| M3 | `RenderMeshBufferInfo` | `RenderMeshBufferInfo` | ✅ | The `Indexed { count, index_format }` / `NonIndexed` discriminator now drives the draw (`indexed()`); index format comes from the metadata, and the index buffer+range comes from `mesh_index_slice`. Non-indexed geometry draws `0..vertex_count` from the vertex slice in both mesh and text draw commands. |
| M4 | `RenderAsset<RenderMesh>`: fallible prepare with previous GPU asset and render-asset usage | `RenderAsset<Mesh>` → `ProcessedAsset = RenderMesh` | ✅ | `Mesh` now stores public per-mesh `asset_usage`, requires it at construction, preserves it through `take_gpu_data`, and returns it from `RenderAsset<Mesh>::usage`. Dual-world extraction uses an independent deep copy while render-only extraction transfers the payload. Primitive factories use the Bevy-default `MAIN_WORLD | RENDER_WORLD`; custom/runtime call sites declare their intended usage explicitly. Morph-image retry remains separately tracked by M8, and exact extracted-state/access errors by M9. |
| M5 | `MeshAllocatorPlugin` and `MeshAllocator`: slab allocation, update, free, buffer slices | `MeshAllocatorPlugin` + `MeshAllocator` | ✅ | The public `allocate_and_free_meshes` system now frees removed/modified allocations, allocates every extracted mesh, grows each affected general slab once, then uploads every payload, matching Bevy's three-phase frame flow. `MeshAllocator` is a move-only ready/finish-time resource with public `extra_buffer_usages` and query methods; implementation state and helper types are private/detail-only. General/large buffer usages, pending/resident transitions, actual-buffer `slabs_size()`, slot-capacity rounding, slab-ID sentinel skipping, and first-growth-capacity retention match Bevy. `general_vertex_slabs_supported` is retained; while wgpu-native's C API lacks Bevy's downlevel-flags query, Epix's temporary Vulkan coercion guarantees `BASE_VERTEX`, so `from_world` records true with a colocated removal note. |
| M6 | `MeshAllocatorSettings` | `MeshAllocatorSettings` | ✅ | Settings are a separate render-world resource initialized by `MeshAllocatorPlugin`, preserved when configured before plugin attachment, and passed explicitly into allocation. `MeshAllocator` no longer owns settings, matching Bevy's resource/API boundary. |
| M7 | `MeshBufferSlice`, `SlabId`, allocation query methods | `render::mesh::{SlabId, MeshBufferSlice}`, `MeshAllocator` query methods | ✅ | The generic allocator value types and allocator implementation both live at the Bevy-shaped render-layer boundary. `MeshBufferSlice` exposes one element `range`, and the real offset-allocator-backed query methods (`mesh_vertex_slice`, `mesh_index_slice`, `mesh_slabs`, `slab_count`, `slabs_size`, `allocations`) are consumed by mesh/text draw commands. |
| M8 | Optional morph target extraction and `MorphPlugin` / `inherit_weights` | none | 🚫 | No morph target, `MorphPlugin`, mesh morph-weight components, or inheritance system exists in Epix. This is not yet accepted as out of scope. |
| M9 | `MeshExtractableData` and `MeshAccessError`: distinguish data, absent data, and data already extracted to the render world | `MeshExtractableData<T>` with `Data`, `NoData`, and `ExtractedToRenderWorld` states | ✅ | Mesh attributes and indices now preserve Bevy's three distinct states. A newly constructed empty mesh extracts successfully exactly once; later access reports `ExtractedToRenderWorld`, while absent indices report `NotFound`. Public fallible/infallible attribute and index accessors, lazy attribute ranges, insertion/removal builders, cloning, and render-asset error mapping follow Bevy's contract. |
| M10 | Built-in `MeshVertexAttribute` IDs and raw packed-layout shader locations | `Mesh::ATTRIBUTE_*` slots and `Mesh::get_mesh_vertex_buffer_layout` | ✅ | Built-in IDs/names/formats and `FIRST_AVAILABLE_CUSTOM_ATTRIBUTE=8` match Bevy. Attribute names are borrowed `std::string_view` data, full attributes convert to IDs, raw packed layouts assign iteration-index shader locations, and `MeshVertexBufferLayout::get_layout` remaps stable IDs to each pipeline's requested shader locations. Mesh2d uses Bevy's position=0, normal=1, uv=2, tangent=3, color=4 interface; text uses custom ID 8 remapped to location 1. Missing attributes return `MissingVertexAttributeError`. |
| M11 | `calculate_bounds_2d`: separate new-mesh insertion from changed-mesh updates | `calculate_bounds_2d` | ✅ | Added Bevy-style `AsAssetId` and `AssetChanged<T>` support in the owning assets module, with change ticks maintained while asset events are flushed. The C++ customization point is the `AsAssetId<T>` template struct and its validating concept is `AsAssetIdImpl`. Bounds are inserted only for meshes missing `Aabb` and updated only when the `Mesh2d` component or referenced mesh asset changes, with Bevy's `NoFrustumCulling`/`NoAutoAabb` exclusions. A render-only mesh is no longer read again after its CPU data has transferred. |
| M12 | `MeshVertexBufferLayouts` structural hash interning and pointer-hashable `MeshVertexBufferLayoutRef` | `MeshVertexBufferLayouts` hash set and pointer-hashable `MeshVertexBufferLayoutRef` | ✅ | The store uses heterogeneous structural hashing/equality to intern layouts with average-constant-time lookup, while returned references compare and hash by their shared pointee address. Structurally equal layouts therefore share one identity exactly as in Bevy. |
| M13 | `BaseMeshPipelineKey` high-bit topology encoding and morph flag | `BaseMeshPipelineKey` | READY FOR REVIEW | Added the Bevy 0.18 key shape and topology helpers in the CPU mesh module. Morph flag behavior cannot be exercised until M8 exists. |
| M14 | `Mesh2d`, `Mesh3d`, `MeshTag`, `AsAssetId` conversions, and `mark_3d_meshes_as_changed_if_their_assets_changed` | matched mesh components and PostUpdate system | ✅ | Both mesh newtypes default to an invalid weak handle, dereference to their handle, convert to `AssetId<Mesh>`, implement `AsAssetId`, compare by value, and require `Transform`. `MeshTag` defaults to zero. `MeshPlugin` schedules the public propagation system after asset-event writing; exactly like Bevy, it responds only to `Modified` events and marks only referencing `Mesh3d` components changed. Bevy handles `Mesh2d` asset changes separately through `AssetChanged<Mesh2d>` specialization invalidation while rebuilding extracted instances each frame; matching that Epix render-side condition remains tracked under S9. |
| M15 | Camera-owned `MeshAabb for Mesh`; dependency direction is camera → mesh | `camera::MeshAabb<mesh::Mesh>` specialization; camera → mesh | ✅ | The mesh member was removed. Camera owns the specialization and implementation, directly includes/imports mesh, and mesh no longer depends on camera. No cross-module forward declaration is used. The implementation and transformed-AABB half-space calculation follow Bevy's componentwise-absolute transform logic. |
| M16 | Full `Mesh` API: topology, attributes, indices, duplicate/merge, transforms, winding, normals/tangents, joints, triangles, asset IDs, extraction state, constants, and final AABB | partial `Mesh` API | REVIEW | Core extraction/access behavior is covered by M4/M9/M10, but the complete public method set and implementation logic have not yet been matched method by method. |
| M17 | `Indices`, `MeshWindingInvertError`, `MeshTrianglesError`, iteration/range conversion, byte views, swap/remove, and winding operations | detailed by M17a–M17d | ❌ | The source-level audit is now split below so individual entities cannot be hidden by successful indexed draws. |
| M17a | `Indices`: U16/U32 alternatives, lazy `iter`, `len`, `is_empty`, `push`/`Extend` promotion, and `IndexFormat` conversion | replacing type-erased `MeshIndices` with `Indices` | READY FOR REVIEW | The new variant-backed type preserves the active width, returns a lazy C++ range, promotes U16 storage on the first value above `u16::MAX` without losing the remainder, and exposes exact Bevy names and format conversion. `Mesh::get_index_buffer_bytes` and all production consumers use the new contract. This touches the render allocator/header consumer and therefore awaits the parent-module checkpoint. |
| M17b | `MeshWindingInvertError` and `MeshTrianglesError` variants, nested `MeshAccessError`, and display text | tagged C++ variants with matching alternatives and messages | ✅ | Rust data enums map to `std::variant`; payloadless variants are distinct marker structs and `MeshAccessError` is retained as the payload-bearing alternative. |
| M17c | `Mesh::duplicate_vertices`, winding inversion, and consuming builder variants | none | 🚫 | Missing algorithms and topology/error behavior. |
| M17d | `MeshTrianglesError` and lazy `Mesh::triangles` across indexed/non-indexed triangle lists/strips | none | 🚫 | Missing typed error and triangle range. |
| M18 | Full `VertexAttributeValues` variant set, conversions/errors, length/emptiness, typed access, raw bytes, and supported-format mapping | type-erased vector representation with a smaller format surface | ❌ | Current representation does not yet prove equivalent accepted types, conversion errors, or format behavior. |
| M19 | `Meshable`/`MeshBuilder`, complete 2D and 3D primitive builders, extrusion/rings, UV modes, anchors, and builder errors | a few convenience factories | 🚫 | Most Bevy primitive construction API and algorithms are missing. |
| M20 | `SkinnedMesh` and `SkinnedMeshInverseBindposes` asset/component support | none | 🚫 | Missing. |
| M21 | `SerializedMesh`, deserializer, `MeshMergeError`, merge/duplicate-by-indices behavior | none | 🚫 | Missing serialization and merge surface. |
| M22 | Optional MikkTSpace tangent generation and typed `GenerateTangentsError` | none | 🚫 | Missing; feature/library availability needs recording, but integration points must remain. |
| M23 | `MeshPlugin` asset registration, inverse-bindpose asset, reflection registration, and PostUpdate asset-change scheduling | partial `MeshPlugin` | REVIEW | Audit plugin ownership and schedule edges after M14/M20 are available. |

## Sprite and Sprite Render

| ID | Bevy entity / behavior | Epix counterpart | Status | Notes |
| --- | --- | --- | --- | --- |
| S1 | Separate `SpritePlugin` (CPU sprite facilities) and `SpriteRenderPlugin` (render composition); render plugin installs `Mesh2dRenderPlugin`, color material, and tilemap plugins | separate `epix_sprite` and `epix_sprite_render` targets/modules with matching plugin roles | ❌ | Physical ownership and the dependency DAG now match Bevy: CPU sprite is below render; sprite and Mesh2d rendering are in the higher sprite-render module. `SpriteRenderPlugin` installs `Mesh2dRenderPlugin` through `app.add_plugins`. Color-material and tilemap plugin composition remain missing under S10/S14, so S1 is not yet complete. |
| S2 | `Sprite`: image handle, optional texture atlas, color, flips, `custom_size`, optional `rect`, `image_mode`; constructors, `compute_pixel_space_point`, required components, visibility-class hook, and `AsAssetId` | matching CPU `Sprite` contract | ✅ | The bundle-era image/component split was removed. Fields, constructors, pixel-space mapping, required components, visibility class, and `AsAssetId` now follow Bevy and have direct tests. |
| S3 | `Anchor` component with nine constants, default center, and vector conversion | `Anchor` component with matching constants and center default | ✅ | Required by `Sprite`; constants and AABB effects are tested. |
| S4 | `SpriteImageMode` and `SpriteScalingMode`, including scale/uses-slices helpers | matched | ✅ | The six proportional scaling modes now apply Bevy's UV crop or fitted-quad transforms during sprite preparation. The dedicated texture-slice example shows all six modes together. |
| S5 | `BorderRect`, `TextureSlice`, `TextureSlicer`, `SliceScaleMode`, computed slices, and asset/change-driven slice systems | matched | ✅ | `ComputedTextureSlices` is maintained from Sprite changes and Image events, extracted through a lazy range, and expanded into per-slice GPU instances. Unit coverage exercises both maintenance paths; the visual example covers nine-slice and tiling. |
| S6 | `calculate_bounds_2d` jointly handles new/changed Mesh2d and Sprite entities in `SpritePlugin` | joint Mesh2d/Sprite bounds system | ✅ | Sprite image/rect/atlas/custom-size/anchor branches and Bevy's disjoint query exclusions are implemented; creation and changed-component updates are tested. |
| S7 | `Text2d`, text layout/update/bounds/shadow API in `bevy_sprite`, plus render extraction integration when text is enabled | text facilities live in `epix_text` | REVIEW | Audit Epix text ownership and public API before deciding the exact C++ module mapping; do not introduce cross-module declarations. Render integration points must remain available. |
| S8 | Optional sprite picking backend, settings, camera marker/mode, hit testing, and plugin | none | 🚫 | Missing. Feature gating/module integration must be assessed against Epix's picking support. |
| S9 | `Mesh2dRenderPlugin`, view specialization caches/ticks, batched instance buffer, exact extracted instance model, bind groups, and draw commands | custom `MeshRenderPlugin` / simplified instance pipeline | ❌ | Physical ownership is corrected to sprite-render, but public names, extraction shape, shader-visible uniforms, scheduling, and batching remain different. Bevy's `ViewKeyCache`/`ViewSpecializationTicks` are not standalone caches: generic `Material2d` queueing consumes their keys/ticks to decide per-view specialization. They must be implemented together with S10 rather than added as unused resources. Bevy's `extract_mesh2d` rebuilds all visible instances each frame; material specialization separately observes `Changed<Mesh2d>` and `AssetChanged<Mesh2d>`. |
| S10 | Generic `Material2d`, `Material2dPlugin`, specialization caches, prepared material properties, `MeshMaterial2d<M>`, and `ColorMaterial` | fixed `MeshMaterial2d` / `MeshTextureMaterial2d` structs | ❌ | The current fixed materials are not Bevy's generic material asset pipeline. |
| S11 | `Wireframe2dPlugin`, phase/item/keys, material/extraction/specialization, config and marker components | none | 🚫 | Missing full 2D wireframe facility. |
| S12 | Sprite render extraction, events, batching, image/view bind groups, phase queueing, and draw commands | custom sprite renderer | REVIEW | Existing rendering works visually, but fields, resources, event handling, batching keys, visibility inputs, schedules, and shader contracts have not yet been compared line by line. |
| S13 | Texture-atlas-aware sprite sizing, UV selection, extraction, and pixel-space conversion | image-owned atlas types consumed by CPU and render sprite paths | ✅ | Atlas region sizing, extraction UV selection, optional sprite rect offset, and pixel-space conversion are implemented and covered by unit and stable three-frame visual checks. |
| S14 | `TilemapChunk`, tile data/storage, mesh cache, material, packing/image creation, plugin systems, and rendering | none | 🚫 | Missing. |
| S15 | `extract_text2d_sprite` emits glyph sprites into the shared sprite render pipeline | separate text mesh renderer | REVIEW | Audit against Bevy's feature-gated integration; preserve direct module dependencies for any cross-module types. |
| S16 | Bevy sprite/mesh2d shader libraries, pipeline layouts, tonemapping/debanding keys, and per-view bind-group contracts | embedded Slang shaders and custom layouts | REVIEW | Slang is intentional, but shader-visible layouts, specialization flags, binding ownership, and processing logic must otherwise match Bevy. |

## Image

The image loader may use Epix's selected C++ decoding libraries and need not
copy Bevy's Rust `image`/Basis/KTX2/DDS implementation. That exception covers
the decoder backend and the set of formats the backend can actually decode;
it does not waive Bevy-facing asset, settings, image-data, atlas, or render
preparation semantics.

| ID | Bevy entity / behavior | Epix counterpart | Status | Notes |
| --- | --- | --- | --- | --- |
| I1 | `Image` owns optional bytes, `TextureDataOrder`, full texture descriptor, optional view descriptor, `RenderAssetUsages`, sampler, and copy-on-resize | private width/height/type/format fields, byte vector plus presence flag, `ImageUsage`, no texture/view descriptors or data order | ❌ | Core data and descriptor shape must match. Direct C++ wgpu descriptor types are allowed, but the represented fields and behavior are not optional. |
| I2 | `Image` constructors and size/resize/reinterpret surface: `new`, `new_uninit`, `new_fill`, target texture, `size`, `size_f32`, aspect ratio, resize-in-place, reinterpret size/stacked arrays, compression query | custom `create*`, value-returning resize, and partial accessors | ❌ | Match Bevy behavior while retaining only clearly useful Epix convenience overloads as extensions. |
| I3 | Pixel offset/byte/color access, clear, typed `TextureAccessError`, conversion errors, and exact dimensional/layer behavior | separate sample/write errors and custom float-array conversion | ❌ | Existing accessors are not equivalent for layers, compressed/unsupported formats, edge bounds, or typed errors. |
| I4 | `ImageSampler` tagged union: `Default` or descriptor payload, linear/nearest helpers, mutable descriptor initialization | enum discriminator plus descriptor stored separately on every `Image` | ❌ | Replace with a payload-carrying C++ tagged union and match `get_or_init_descriptor`. |
| I5 | Full sampler descriptor and conversion surface, including anisotropic helper and `OpaqueWhite`/`Zero` border variants | mostly matching fields and filter/address helpers | ❌ | Add missing variants/helper and exact conversion behavior; direct wgpu C++ types are the accepted translation. |
| I6 | `TextureAtlasPlugin`, `TextureAtlasLayout`, `TextureAtlas`, and `TextureAtlasSources` with grid/layout/look-up helpers | image-owned matching atlas facilities | ✅ | Grid ordering/extents, layout mutation, selected rectangles, source index/rect/UV/handle lookup, plugin installation, and actual sprite rendering are tested. |
| I7 | `TextureAtlasBuilder`, typed packing errors, format conversion, padding, atlas-source mapping, and output image | none | 🚫 | Requires a faithful packing implementation or an equivalent C++ packing dependency; do not invent a different packing contract. |
| I8 | `DynamicTextureAtlasBuilder`, incremental allocator, padding, copy placement, and typed errors | none | 🚫 | Needed for procedural/font atlas parity. |
| I9 | `ImagePlugin` registers images/fallbacks and preregisters the loader; render-side image preparation remains composed by the render layer | registers images, loader, and fallbacks directly | ❌ | Preserve the existing explicit `AssetPlugin` prerequisite. Match plugin ownership and registration timing supported by Epix's asset architecture. |
| I10 | `ImageLoaderSettings`: format selection/guessing, optional texture format, sRGB, sampler, asset usage, and array layout; typed loader errors | empty settings and a fixed stb-based decode path | ⚠️ | Decoder implementation and actually supported file formats intentionally differ. The settings and post-decode behavior still need to match wherever the selected backend can support them. |
| I11 | Public format/MIME/extension mapping, texture errors, compressed-format capability selection, and optional EXR/HDR/KTX2/DDS/Basis paths | smaller runtime `Format` enum and stb extension list | ⚠️ | Loading-library/capability divergence accepted. Keep explicit, typed unsupported-format results and retain integration points for formats the chosen backend cannot decode. |
| I12 | `SerializedImage` feature for short-lived cross-process transmission | none | REVIEW | Optional feature; retain an integration point if serialization support is not implemented in the current pass. |
| I13 | Render extraction/preparation respects descriptors, view descriptors, data order, mip/layer layout, previous-image copy-on-resize, and retry errors | `RenderAsset<Image>` / `GpuImage` with simplified metadata | ❌ | Re-audit after I1-I5 because changing the CPU contract changes the prepared GPU-image inputs. Direct wgpu resource ownership remains intentional. |

## Core Pipeline Plugin and Shared Facilities

| ID | Bevy entity / behavior | Epix counterpart | Status | Notes |
| --- | --- | --- | --- | --- |
| C1 | `CorePipelinePlugin`: installs 2D, 3D, blit, tonemapping, upscaling, OIT, mip generation, deferred-copy plugins | `CoreGraphPlugin` | ❌ | `CoreGraphPlugin` installs Core2D, blit, upscaling, and tonemapping and initializes the shared fullscreen shader. Core3D, deferred-copy, OIT, and mip-generation composition remain missing. The Core2D tonemapping node is no longer empty. |
| C2 | `FullscreenShader`: shared embedded fullscreen-triangle vertex shader and `to_vertex_state()` | `core_graph::FullscreenShader`, initialized by `CoreGraphPlugin` | ✅ | CoreGraphPlugin directly mirrors Bevy CorePipelinePlugin ownership: embeds the shared shader, installs core plugins, then initializes the render-world resource. Core2D only consumes it. Slang remains the accepted shader-language divergence. |
| C3 | `FullscreenMaterial<T>` and `FullscreenMaterialPlugin<T>`: extracted uniform component, two HDR/non-HDR pipelines, a typed view node, and explicit graph edges | `core_graph::FullscreenMaterial`, `FullscreenMaterialPlugin<T>`, `FullscreenMaterialNode<T>`, `FullscreenMaterialPipeline` | ❌ | The material contract now returns `ShaderRef`, render startup resolves handle/path references through the render-world `AssetServer`, the pass uses Bevy's default load operation, and the LDR pipeline uses the Bevy-default sRGB target format. The Camera2d path is source-, test-, and visually verified. Full parity remains blocked only by Camera3d automatic graph integration, which depends on the missing C13 Core3d graph. |
| C4 | General `BlitPlugin`, `BlitPipeline`, and `BlitPipelineKey` | `core_graph::BlitPlugin`, `BlitPipeline`, and `BlitPipelineKey` | ✅ | Reusable core-graph facility now owns an embedded fragment shader, render-startup resource initialization, specialized-pipeline cache, direct-WGPU bind-group creation, and format/blend/sample specialization. Core2D's private output blit remains separately audited under C17. |
| C4a | `SpecializedRenderPipeline` / `SpecializedComputePipeline` specialize from `&self` | `render::SpecializedRenderPipeline` / `render::SpecializedComputePipeline` concepts | ✅ | Pipeline instances own `Key` and `specialize(key)`, and cache methods receive the specialization instance exactly as in Bevy. |

## Core 2D Pipeline

| ID | Bevy entity / behavior | Epix counterpart | Status | Notes |
| --- | --- | --- | --- | --- |
| C5 | `Core2dPlugin` required components, phase extraction, depth preparation, graph registration | `Core2dPlugin` | ✅ | The plugin now adds Bevy's `DebandDither::Disabled`, `CameraRenderGraph{Core2d}`, and `Tonemapping::None` requirements to `Camera2d`, and registers `ExtractComponentPlugin<Camera2d>`. Phase/depth/graph details remain separately tracked under C6–C12 and C17; the actual tonemapping pipeline is C16. |
| C6 | `core_2d::graph::Core2d` and all graph node labels | `Core2dGraph`, `Core2dNodes` | ✅ | `Core2dNodes` now exposes Bevy's complete public label set. Core2D installs exactly Bevy's eight default nodes and chain: main passes, post-processing endpoints, tonemapping integration point, and upscaling. MSAA writeback, wireframe, bloom, general post-processing, FXAA, SMAA, and CAS remain labels for their owning optional plugins. The actual tonemapping node implementation is C16. |
| C7 | Binned `Opaque2d`, `AlphaMask2d`, their bin keys, and batch-set key | Binned `Opaque2D` / `AlphaMask2D`, `BatchSetKey2D`, and mesh producer | ✅ | Core2D now uses two Bevy-shaped binned phase types. Mesh extraction writes `RenderMesh2dInstances`, keyed by `MainEntity` exactly as Bevy, so placeholder binned render entities never become an implicit data dependency. The C++ `MeshAlphaMode2d` tagged union covers `Opaque`, `Mask(cutoff)`, and `Blend`; mask pipelines discard below the cutoff and retain depth writes. |
| C8 | Sorted `Transparent2d` phase item | `Transparent2D` | ✅ | Entity, main entity, cached pipeline, draw function, range, extra index, reverse-depth sorting, and the `indexed` discriminator now correspond. Mesh queueing reads `GPUMesh::is_indexed`; sprite and text producers explicitly report non-indexed geometry. |
| C9 | `extract_core_2d_camera_phases` maintains opaque, alpha-mask, transparent phase resources | `extract_core_2d_camera_phases` | ✅ | Re-audited and corrected: public helper with Bevy's name, active Camera2d query independent of CameraRenderGraph, Local live-view set reuse, transparent clearing, both binned phases prepared with None, and dead-view retention. Core-graph-owned tests cover custom/missing graphs and inactive/despawned cameras. |
| C10 | `prepare_core_2d_depth_textures` cached depth textures per target and MSAA samples | `prepare_core_2d_depth_textures` (Core2D) | ✅ | Re-audited and corrected: explicit With<Camera2d> on the synced view entity, both phase-membership checks, optional-target cache key (including None with a physical size), and public CORE_2D_DEPTH_FORMAT. GPU regression covers rejected non-2D/missing-phase/missing-size views, shared targets, MSAA, depth format/usage, and first-use reverse-Z clearing over two frames. Scheduled in PrepareResources; generic ViewPlugin does not allocate depth. |

| C11 | `MainOpaquePass2dNode` with independent opaque + alpha-mask pass work | `MainOpaquePass2DNode` | ✅ | The dedicated typed view node resolves both binned phases and records them, in order, into one deferred generated command buffer with Bevy-matching color/depth attachments and camera viewport. |
| C12 | `MainTransparentPass2dNode` clears/render transparent pass independently | `MainTransparentPass2DNode` | ✅ | A separate typed view node records the retained sorted transparent phase into its own deferred generated command buffer. WebGL-only viewport reset remains irrelevant to the forced Vulkan path. |

## Core 3D and Pipeline Stages

| ID | Bevy entity / behavior | Epix counterpart | Status | Notes |
| --- | --- | --- | --- | --- |
| C13 | `Core3dPlugin` base pipeline | none | 🚫 | Umbrella item for C13a-C13d. `Camera3d`, `Camera3dDepthLoadOp`, `Camera3dDepthTextureUsage`, and transmission settings already correctly live in `epix_camera`, matching Bevy 0.18's `bevy_camera`; the missing ownership begins at the core-pipeline plugin. |
| C13a | `core_3d::graph::{Core3d, Node3d}`, four main 3D phase types/keys, plugin resources, extraction, and sorting | none | 🚫 | Must add all Bevy public node labels, `Opaque3d`, `AlphaMask3d`, `Transmissive3d`, and `Transparent3d`; initialize their draw/phase resources; extract active Camera3d views; select the preprocessing mode from `GpuPreprocessingSupport` and `NoIndirectDrawing`; retain live views; and sort both sorted phases. Exact `Opaque3dBatchSetKey` parity is currently gated by P6. The alpha-mask key also shares Bevy prepass key types, so C13a and the corresponding C14 public key surface must be staged together rather than duplicated. |
| C13b | `prepare_core_3d_depth_textures`, `ViewTransmissionTexture`, and `prepare_core_3d_transmission_textures` | none | 🚫 | Depth preparation must union each target's requested `Camera3dDepthTextureUsage`, add `COPY_SRC` for prepass consumers, cache by target plus MSAA, and honor clear-vs-load. Transmission preparation must require all four phases, nonempty transmissive work, a nonzero step count and physical size, cache one single-sample copy-destination/sampleable texture per target in the view HDR/LDR format, and create the linear sampler. |
| C13c | Main opaque, transmissive, and transparent pass nodes plus the default Core3D graph chain | none | 🚫 | Must register Bevy's default nodes and integration endpoints, render opaque then alpha-mask against the shared depth target, split transmissive work into at most the configured number of back-to-front ranges with a source copy before each range, and render transparent last. Missing C14/C15/C20 implementations do not justify deleting their labels or graph extension points. |
| C13d | Core3D unit/GPU/visual verification | none | 🚫 | Add focused phase lifetime/sort/mode tests, depth usage/load/cache tests, transmission allocation/skip tests, graph-node/edge tests, and a dedicated visual example that proves the distinct main 3D phase ordering without changing an existing example's meaning. Capture the actual client HWND across at least three stable frames. |
| C14 | Prepass components, textures, nodes, and preparation | none | 🚫 | No core-graph prepass implementation. |
| C15 | Deferred phases, nodes, and lighting-id copy plugin | none | 🚫 | No core-graph deferred implementation. |
| C16a | `Tonemapping` / `DebandDither` components, camera-filtered extraction, and prepare scheduling | `Tonemapping`, `DebandDither`, `TonemappingPlugin` | ✅ | The value-initialized method is Tony McMapface, Core2D explicitly requires `None`, extraction is filtered by `Camera`, and the synced render entity now also owns `ViewTarget`, so the former two-entity placement mismatch is gone. Pipeline preparation runs in `RenderSystems::Prepare`. |
| C16b | `TonemappingPipelineKey`, specialization, and per-view preparation query | `TonemappingPipelineKey`, `prepare_view_tonemapping_pipelines` | ✅ | The key contains only debanding, method, and typed grading flags; specialization always targets `ViewTarget::TEXTURE_FORMAT_HDR`; and the prepare query uses `With<ViewTarget>` without fetching it as data. Missing-LUT diagnostics are irrelevant in the default-enabled real-LUT configuration, matching Bevy's feature-enabled branch. |
| C16c | `TonemappingNode` resource assumptions and bind-group cache | `TonemappingNode` | ✅ | Required resources use required access, while `None`, non-HDR views, and unavailable pipelines retain Bevy's early returns. The five-entry bind group is cached by view-uniform buffer, source view, LUT view, and method, including Bevy's fallback-LUT invalidation rule. Parallel command generation remains the accepted rendering-executor adaptation. |
| C16d | Default-enabled real LUT assets plus LUT construction/binding helpers | Bundled `TonemappingLuts`, KTX2 decode, and LUT helpers | ✅ | Epix embeds Bevy 0.18's three KTX2 files and decodes their Zstandard-supercompressed RGBA16F/RGB9E5 payloads to filterable RGBA32F 3D images. `get_lut_bindings`, `get_lut_bind_group_layout_entries`, and the render-only `lut_placeholder` match Bevy's public surfaces; Bevy's private setup helper remains internal. A plugin test verifies distinct 32³ AgX, 48³ Tony, and 64³ Blender images, sampler state, format, usage, and payload sizes. |
| C16e | Tonemapping/color-grading shader behavior and HDR visual verification | Embedded Slang tonemapping shader and `tonemapping` example | ✅ | The Slang shader ports Bevy's methods, grading branches, and dithering path. A dedicated 4×2 HDR example renders all eight algorithms simultaneously from one continuously changing HDR clear color through camera `Tonemapping` components; its title and startup/periodic logs state the exact algorithm-to-region mapping. Four direct client captures show eight non-placeholder, method-distinct tiles and distinct frame hashes as the source changes. Slang remains the accepted shader-language divergence. |
| C16f | Direct all-pipeline natural-image visual verification | `tonemapping_hdr_images` example and CC0 HDR assets | ✅ | A separate one-camera Core2D graph node invokes all eight production `TonemappingPipeline` specializations in one 4×2 render pass. The source continuously cycles through three 1K HDR environments loaded through `AssetServer`; every tile samples the identical source without requiring sprite or mesh. Its title and logs state the active source and exact algorithm-to-region mapping. The animated over-range clear-color path remains exclusively in C16e. |
| C17 | Upscaling plugin, per-view pipeline, and node | `UpscalingPlugin`, `ViewUpscalingPipeline`, `UpscalingNode` | ✅ | Top-level plugin specializes the shared `BlitPipeline` per view during `Prepare`, stores the selected ID on the view, and provides a typed deferred-command node under `Core2dNodes::Upscaling`. It preserves Bevy clear color, camera-output mode, later-camera alpha blend, viewport scissor, and source-view bind-group caching. The render-world, move-only `PipelineServer` owns its PipelineCache-equivalent state directly and uses Bevy's `block_on_render_pipeline` before the node runs; the main world has a separate server for explicit main-world GPU work. Direct wgpu texture-view identity is the accepted representation difference. |
| C18 | OIT settings, buffers, resolve pipeline/node | none | 🚫 | No OIT subsystem. |
| C19 | Experimental depth mip-generation plugin and resources | none | 🚫 | No depth-pyramid/mip-generation subsystem. |
| C20 | Skybox pipeline and prepass integration | none | 🚫 | No core-pipeline skybox subsystem. |
| C21 | Shared view shader module namespace follows its import path (`bevy_render::view::{View, ColorGrading}`) | Slang module `epix.view` | ✅ | The Slang module now exports `View` and `ColorGrading` from `epix::view`, matching Bevy's shader-module namespace and Epix's WGSL `epix::view` import path. Mesh, sprite, text, and tonemapping use the qualified names consistently. A real PipelineServer regression compiles an importing Slang entry point; all affected visual pipelines were captured across three stable frames. |

## Cross-module Integration Findings

| ID | Area | Status | Notes |
| --- | --- | --- | --- |
| P1 | `TaskPool::Scope` completion and failure handling used by `RenderContext::finish` | ✅ | Long-running mesh rendering exposed a lost-wakeup race: scoped workers changed the atomic pending count without holding the condition-variable mutex, so the final notification could occur between the waiter's predicate check and sleep. The mixed protocol now uses `std::atomic::wait`/`notify_one`; task exceptions are captured and rethrown after all scoped work completes, matching Bevy's scoped-task failure propagation. |
| P2 | Empty `async_channel::recv` scheduling | REVIEW | The supplied active-worker stacks are IO-pool asset tasks repeatedly rescheduling an empty receive at `async_channel.hpp:300`, rather than threads blocked in the render failure. This busy polling consumes the IO workers and needs a separate parent `utils`/asset integration audit; suspending those workers after a render freeze did not unblock `RenderContext::finish`, so it is not the P1 stall's immediate cause. |
| P3 | LDR `ViewTarget` main-texture format | ✅ | The LDR main texture and its default view now use `RGBA8UnormSrgb`, matching Bevy 0.18's `TextureFormat::bevy_default()`; the linear twin remains an allowed alternate view format. A direct GPU regression checks both the component and underlying texture format. |
| P4 | Render-world `AssetServer` availability | ✅ | Render initialization clones the main-world `AssetServer` resource into the render world, matching Bevy's `initialize_render_app`. This enables render-startup systems such as `FullscreenMaterialPlugin` to resolve `ShaderRef::Path` without an invented source-registration API. |
| P5 | Render-only `Image` extraction | ✅ | `Image` now preserves Bevy's `Option<Vec<u8>>` present/absent distinction and `RenderAsset<Image>::take_gpu_data` transfers render-only pixels while retaining source metadata. Repeated extraction follows Bevy's `had_data`/previous-GPU-image rule, and prepare skips uploads only when the payload is absent—not merely empty. Image and render parity tests cover metadata retention, empty-present data, one-shot extraction, retry validity, and byte accounting. This parent-render fix is required for C16's render-only LUT assets. |
| P6 | Core3D phase dependency on mesh slab identity | READY FOR REVIEW | `render::mesh::{SlabId, MeshBufferSlice}` now owns the generic value types at the same lower render-layer boundary as Bevy, and `epix_mesh` consumes them without aliases or duplicate Core3D types. `MeshBufferSlice` uses Bevy's single `range` shape. This removes the otherwise circular C13 dependency while keeping the allocator implementation in the mesh module. Full clean build, focused tests, and mesh/text visual verification pass; commit awaits user approval. |

## Audit Log

Earlier log entries describe what was claimed at that checkpoint. Revised rows
above supersede unsupported acceptance/parity claims; an implementation is not
an accepted divergence merely because it was previously committed.

| Date | Evidence | Result |
| --- | --- | --- |
| 2026-09-08 | P6/M7: compared Bevy 0.18 `bevy_render/src/mesh/allocator.rs` ownership with Epix's render/core-graph/mesh dependency direction. Reconfigured, cleaned 1,409 outputs, and rebuilt the complete 1,051-action no-modules tree at `--parallel 10`. Ran six affected render/core-graph/mesh/text test executables successfully. | Moved `SlabId` and `MeshBufferSlice` to `epix::render::mesh`, changed the slice API to one element `range`, and updated allocator, mesh drawing, text drawing, and tests without compatibility aliases. Four direct `PrintWindow(PW_CLIENTONLY)` captures each of the actual mesh-rendering, 3,300-item mesh-batching, and text font-image GLFW client windows remained live and non-black; first/last frames were visually inspected and all twelve hashes were distinct due to the updating performance overlay. |
| 2026-09-07 | P3/P4 and C3 follow-up: compared Bevy 0.18 `bevy_render/src/lib.rs`, `bevy_render/src/view/mod.rs`, and `bevy_core_pipeline/src/fullscreen_material.rs` directly with Epix. Reconfigured, cleaned, and rebuilt the affected no-modules render/core targets and downstream mesh, sprite, text, and ImGui examples at `--parallel 10`. Ran 8 render-smoke, 148 render-parity, and 9 core-pipeline tests successfully. | Render initialization now supplies the render-world `AssetServer`; LDR view targets use Bevy's sRGB default; and `FullscreenMaterial` consumes `ShaderRef`, resolves paths through the asset server, and loads the existing target instead of clearing it. Three actual process-owned render-window captures each of fullscreen material, mesh rendering, sprite pressure, text interactive, and the ImGui render plugin were non-black, visually correct, and actively presented. The pass initially exposed a separate `text_interactive` example bug: continuously replaced outline meshes were incorrectly `RENDER_WORLD`-only, so the close frame could make bounds calculation access an extracted payload. Retaining both main- and render-world data fixed it; three repeated captures and the close path then completed without errors. This ordering also proves the diagnostic was not a QueryIter regression. C3 remains open only for its C13-dependent Camera3d graph integration. |
| 2026-09-07 | M12/C3 follow-up: compared Bevy 0.18 `bevy_mesh/src/vertex.rs` and `bevy_core_pipeline/src/fullscreen_material.rs` directly with Epix. Reconfigured, cleaned, and rebuilt the no-modules mesh test plus rendering/batching examples at `--parallel 10`; all 33 mesh tests passed. | `MeshVertexBufferLayouts` now has Bevy's structural hash-set interning and `MeshVertexBufferLayoutRef` has pointer hashing. Three captures each of the actual mesh-rendering and 3,300-item batching GLFW client windows were visually correct. Separately, the fullscreen-material window remained responsive for 185 seconds with four valid captures and no logged errors, closing the old runtime-stability finding while retaining its source/API mismatches. The comparison also exposed P3 rather than hiding the render target's format mismatch inside the core pipeline. |
| 2026-09-07 | P1: reproduced the freeze on detached parent commit `b9d99348`, ruling out the QueryIter change; watchdogs and native stacks located the blocked render thread in `RenderContext::finish` while worker tasks had completed. A timed diagnostic wait eliminated the permanent stall, proving a missed condition-variable notification. Reconfigured, cleaned, and rebuilt the no-modules task tests plus mesh rendering/batching targets with MSVC 14.52 at `--parallel 10`; all 36 task tests passed, including 10,000 rapid scopes and thrown-task propagation. | The parent-task fix uses the pending atomic itself as the wait primitive, so completion cannot be lost between predicate inspection and suspension. Production mesh batching and mesh rendering each remained live for five minutes; three direct captures of each process-owned render HWND had distinct full-frame hashes due to the changing performance overlay, and visual inspection showed the expected non-black scenes. |
| 2026-09-06 | M10/M11: compared local Bevy 0.18 `bevy_mesh/src/{vertex.rs,mesh.rs}`, `bevy_sprite/src/lib.rs`, and `bevy_asset/src/{asset_changed.rs,assets.rs}` with Epix. Reconfigured and clean-built the complete 994-action no-modules tree with MSVC 14.52 at `--parallel 10`; all 14 asset test executables plus 33 mesh, 148 render-parity, 7 render-smoke, 9 core-graph, 3 embedded-text-shader, and 1 text-shaping test cases passed. | Vertex IDs and pipeline-specific shader-location remapping now match Bevy, and generic asset-change filtering gates `calculate_bounds_2d` so render-only meshes are not re-read after extraction. Direct regressions also verify mutable component access with an `AssetChanged` filter and full-attribute conversion in layout membership checks. Three actual process-owned GLFW client captures each of mesh rendering, alpha mask, 3,300-item batching, and text font image were visually inspected and correct. Each scene crop was byte-stable across all three frames (`DC03B3BB...`, `10805A4B...`, `FBE19150...`, `478767A5...`); logs contained no error, fatal, validation, panic, or exception matches. |
| 2026-09-06 | M11 API follow-up exposed a parent assets/ECS mismatch while comparing `bevy_asset::Assets::asset_events`. | `AsAssetId` is now a C++ specialization struct validated by `AsAssetIdImpl`; `Mesh2d` no longer owns an invented member API. Epix ECS gains Bevy's single read-only `SystemChangeTick` parameter (`last_run()`/`this_run()`), and asset changes record its actual `this_run()` tick. The broader preliminary asset re-audit is deferred in `documentation/tmp/BEVY_ASSET_MATCH_STATUS.md`. Reconfigured and clean-built the full 994-action no-modules tree at `--parallel 10`; all 14 asset test executables plus 2 ECS-system, 33 mesh, 148 render-parity, 7 render-smoke, 9 core-graph, 3 embedded-text-shader, and 1 text-shaping test cases passed. The render-smoke and core-graph executables were rerun separately after the first CTest `-j10` invocation exhausted process-level GPU resources; both passed without test failures. Three actual process-owned GLFW client captures each of mesh rendering, alpha mask, 3,300-item batching, and text font image were visually inspected and correct. Scene-only crops were byte-stable across every frame (`750260A3...`, `2F78E408...`, `731E737F...`, `8D3F7990...`), and logs contained no error, fatal, validation, panic, or exception matches. |
| 2026-09-05 | M6: compared Bevy 0.18 `mesh/allocator.rs` resource initialization and allocation-system parameters with Epix. Added a plugin/resource regression, then performed a full no-modules MSVC reconfigure, clean (1,349 outputs), and complete 994-action rebuild at `--parallel 10` after a partial relink caused the text example to access-violate. Ran 28 mesh tests, 3 embedded-shader-loading tests, and 1 text-shaping test. | `MeshAllocatorSettings` now lives independently in the render world and is passed to allocator operations. Three process-owned GLFW client captures each of mesh rendering, alpha-mask, batching, and text font-image were visually inspected and stable; logs contained no error, fatal, validation, panic, or exception output. The clean rebuild eliminated the stale-ABI text crash. |
| 2026-09-05 | C21: compared Bevy's `bevy_render::view::{View, ColorGrading}` imports with Epix's WGSL and Slang view modules, then audited every production `import epix.view` consumer. Built affected no-modules MSVC targets at `--parallel 10`; 195 focused tests passed, including a new real Slang/PipelineServer namespace-compilation regression. | Moved the Slang exports from flat `epix` to `epix::view` and updated mesh, sprite, text, and tonemapping atomically. Three process-owned GLFW client captures each of mesh rendering, sprite basic, text font-image, and fullscreen material were visually inspected and stable; logs contained no shader, validation, panic, or runtime errors. |
| 2026-09-05 | Resumed from clean HEAD 6d3514b9; re-read local Bevy core_2d functions and audited new tracker claims. | Reopened M4-M6, C3, C16 and render N33-N34 rather than inheriting unsupported accepted labels. Corrected C1's stale plugin inventory. |
| 2026-09-05 | C9/C10: no-modules reconfigure and affected-target rebuild at --parallel 10; 11 CTest executables (235 Google Tests), zero failures/skips. | Phase/depth regressions pass on the GPU. Tests now live with their owning modules; render tests no longer link core_graph. Three process-owned GLFW client captures inspected for each of mesh rendering/alpha-mask/batching, sprite basic/pressure, text font-image/interactive, ImGui basic, blit, and custom render-plugin examples. Scene content was stable; the live performance overlay varied. Captures/logs: tmp/core2d-depth-20260905. Final public-name/constant rebuild followed by the same tests and three fresh mesh captures also passed. |
| 2026-09-05 | C3 runtime checks: two capture launches and one no-capture launch of examples_header_render_glfw_fullscreen_material. | All three became unresponsive. Initial capture showed two dark-red frames before stalling; without any capture, the window stopped responding after the first responsiveness sample and continued consuming CPU. Processes were terminated after bounded checks. Root cause remains undiagnosed; C3 stays open. |
| 2026-08-31 | Read all source-file inventories, `bevy_render::mesh`, `bevy_core_pipeline::lib`, and `core_2d` public declarations directly from local Bevy 0.18.0 source. | Initial numbered inventory created. |
| 2026-08-31 | C2: full no-modules reconfigure/rebuild; `tests_header_render_render_parity_tests` and `tests_header_mesh_module_test`; three actual GLFW client-area captures of `examples_header_mesh_rendering`. | Shared `FullscreenShader` fixed and visually verified. |
| 2026-08-31 | C2 ownership and shader-coordinate correction: targeted no-modules rebuild, the same two focused tests, and three fresh GLFW client-area captures of `examples_header_mesh_rendering`. | `CoreGraphPlugin` owns initialization exactly as Bevy `CorePipelinePlugin`; fullscreen-triangle UV and clip-space mapping match Bevy. |
| 2026-09-01 | C3/C4 source audit against Bevy `fullscreen_material.rs` and `blit/mod.rs`; C4a targeted no-modules build and `tests_header_render_render_parity_tests`. | Documented the absent generic facilities and corrected the shared specialized-pipeline instance contract. |
| 2026-09-01 | C4: read Bevy `blit/mod.rs` and `blit.wgsl` directly; implemented the equivalent core-graph plugin, pipeline, key, bind group, and specialization contract. Reconfigured the no-modules build, ran `tests_header_render_render_parity_tests`, and built `examples_header_render_glfw_blit_pipeline`. | The focused test passed. Three actual GLFW client-area captures used a red camera/output fallback and blue intermediate source; all were stable at 960x540 with identical center BGR `(246,173,56)`. The visible blue therefore proves that BlitPipeline overwrote the red output fallback. |
| 2026-09-01 | C5: read Bevy `core_2d/mod.rs`, `main_opaque_pass_2d_node.rs`, and `main_transparent_pass_2d_node.rs` against Epix `core2d.cpp` and `core2d.hpp`; implemented the missing Core2D camera requirements and `Camera2d` extraction. | `tests_header_render_render_parity_tests` passed with a focused test that verifies the main-world defaults and the render-world marker after the deliberate ExtractCommands deferred-application stage. A rebuilt `examples_header_mesh_rendering` produced three actual GLFW client-area captures at 1280x720; their scene-only hashes were identical and showed the expected opaque, gradient, and transparent geometry. Graph, phase, depth, and upscaling sub-mismatches remain separately numbered. |
| 2026-09-01 | C6: read Bevy `core_2d::graph`, camera-driver graph invocation, and Epix graph runner/context plus `Core2dGraph`. | Confirmed that the view entity is graph-context state in both engines, not a Core2D input slot; this established the public-label/default-chain work completed below. |
| 2026-09-01 | C6: matched Bevy's `Node2d` public label set and the default Core2D graph node chain, while retaining optional-plugin node ownership. | The focused `Core2dGraph.MatchesBevyDefaultNodesAndChain` test verifies all default nodes, all extension-only absences, and every default edge; `tests_header_render_render_parity_tests` also passed. Rebuilt `examples_header_mesh_rendering` produced three GLFW client captures at 1280x720 with identical scene-only pixels below the dynamic overlay. C16 still replaces the tone-mapping placeholder with its GPU node. |
| 2026-09-01 | C7-C10: read Bevy `core_2d/mod.rs` and both Core2D pass nodes against Epix `core2d.hpp/.cpp`, `render_phase.hpp`, `binned_phase.hpp`, `batching.hpp`, `view.cpp`, and mesh queueing. | Epix's generic binned-phase support is already suitable; the remaining work is Core2D/mesh adoption, an indexed transparent field, Core2D-owned phase lifetime, and moving depth preparation to the matching plugin/schedule. |
| 2026-09-01 | C8: added Bevy `Transparent2d::indexed` parity to the shared Core2D sorted phase. | Mesh queues its actual `GPUMesh::is_indexed()` value; sprite and text queue their non-indexed geometry explicitly. The focused phase-item test and `tests_header_render_render_parity_tests` pass. Mesh rendering/batching, sprite basic/pressure, and text font-image/interactive all rebuilt and each produced three actual GLFW client captures with identical scene-only pixels beneath the dynamic overlay. |
| 2026-09-01 | C11-C12/C17: read Bevy Core2D opaque/transparent nodes and `upscaling/mod.rs` plus `upscaling/node.rs` against Epix `Node2D` and `Core2dBlitNode`; confirmed Epix `RenderContext::add_command_buffer_generation_task` and its focused parallel-execution test. | Pass attachment behavior is broadly correct, but the phase/node split is not. Core2D nodes should use the already available deferred command-buffer-generation path. Core2D-private output blitting must become the general-blit-based `UpscalingPlugin` path, including per-view prepared pipeline selection and its required pipeline wait. |
| 2026-09-01 | C17: implemented `UpscalingPlugin`, `ViewUpscalingPipeline`, and typed `UpscalingNode`; removed Core2D-private shader/pipeline/UI ownership. Full no-modules reconfigure/rebuild, `tests_header_render_render_parity_tests`, and three GLFW client-area captures of `examples_header_mesh_rendering`. | Focused tests passed. The three 1280x720 render-window captures had identical SHA-256 `33D7160E8843C124FF1E7F7B89B987646493D86F513C7663C9864C7B85404D4D` and center ARGB `-3684957`; inspected capture showed the expected opaque, transparent, and textured meshes. PipelineServer was subsequently made move-only and world-local, restoring Bevy's `block_on_render_pipeline` for the render-world upscaling preparation path. |
| 2026-09-01 | C17 follow-up: removed `PipelineServerData` indirection; inserted independent move-only PipelineServer resources in the main and render worlds; restored only `block_on_render_pipeline`; made `waiting_pipelines()` a non-owning range. Performed a no-modules CMake reconfigure, Ninja clean (1,285 outputs), and rebuild at `-j10`. | The freshly rebuilt `tests_header_render_render_parity_tests` passed. Three `PrintWindow` captures targeted the actual `GLFW30` “Mesh Rendering Visual Test” window rather than its console; all showed the expected 2D mesh scene. Frame hashes differ only because the live NVIDIA frame-rate overlay changes, not because the scene is white or missing. |
| 2026-09-01 | C17 parity correction: compared `prepare_view_upscaling_pipelines` directly with Bevy 0.18. | Epix now uses the same per-prepare output-texture set to select later-camera alpha blending, and does not omit `Skip` views from pipeline preparation. `specialize`, `block_on_render_pipeline`, and the per-view component insertion deliberately run every Prepare frame in both engines; specialization only queues a pipeline on a cache miss. `tests_header_render_render_parity_tests` passed. |
| 2026-09-01 | C17 final verification: rebuilt `examples_header_mesh_rendering` and `examples_header_render_glfw_blit_pipeline` at `-j10`. | `PrintWindow(PW_CLIENTONLY)` captured the actual `GLFW30` client windows, never their consoles. The mesh example visibly contained its opaque square, both textured gradients, and overlapping transparent circle/rectangle; scene-only frames 2–3 had identical SHA-256 `26CDDE02B781E6AD2F3F65996BDBDF2EF2BC0C63BCBCCA6FA6B25CBF6AC05E77`. The blit example was uniformly blue, proving the source overwrote its red output fallback; scene-only frames 2–3 had identical SHA-256 `38EEAC9AD5F9D726F0A66C1BB84651B990062BB1B4E75507B3BA9462B696F2F6`. The uncropped first frames and all overlay regions vary only because of the live NVIDIA performance overlay. |
| 2026-09-01 | C13: read Bevy `core_3d/mod.rs` and its graph/plugin registration against all Epix core-graph sources. | Core3D has no Epix counterpart: Bevy's 3D graph labels, Camera3D lifecycle, opaque/alpha-mask binned phases, transmissive/transparent sorted phases, depth/transmission preparation, and main-pass nodes are all still missing. |
| 2026-09-02 | C7/C9/C11/C12: compared Bevy 0.18 `core_2d/mod.rs`, `main_opaque_pass_2d_node.rs`, `main_transparent_pass_2d_node.rs`, and `bevy_sprite_render::mesh2d::mesh.rs` with Epix Core2D and mesh producers. Built the no-modules targets at `-j10`; ran 5 mesh tests and 13 Core2D/binned tests. | Added binned opaque and alpha-mask phases, main-entity keyed mesh instances, tagged alpha modes, and the two dedicated deferred pass nodes. A Slang reflection check verified the 96-byte structured-buffer stride; the C++ element now has matching tail padding. Three `PrintWindow(PW_CLIENTONLY)` captures of the actual `GLFW30` alpha-mask window have identical scene-only SHA-256 `25A07F31885DFA24F433F38C640BA409D1A5915BD439AFC390C7F62F6F5677D5` below the dynamic overlay and visibly show the cyan/orange alpha checker. |
| 2026-09-03 | N28: read Bevy `render_phase/binned/mod.rs::add` and implemented the `(Entity, MainEntity)` representative pair as one `std::pair` parameter; migrated the Core2D/mesh queue (`queue_meshes_2d_opaque`), `binned_phase_geometry` example, and all binned-phase focused tests. Rebuilt the no-modules `tests_header_render_render_parity_tests`, `tests_header_mesh_module_test`, and the `examples_header_mesh_rendering`/`alpha_mask`/`batching` examples via the insider MSVC 14.52 + `cmake --build` path. | `render_parity_tests` (189) and `mesh_module_test` (5) passed. Actual GLFW client-area captures (`PrintWindow` client-only) at 1280x720 showed the mesh-rendering scene (opaque box, two gradient quads, overlapping transparent circle/rectangle), the cyan/orange alpha checker at 960x540, and hundreds of colored batched shapes — all stable across frames (frames 1–2 identical for alpha-mask and batching). |
| 2026-09-03 | C10: compared Bevy `prepare_core_2d_depth_textures` (Core2dPlugin::PrepareResources, per-target dedup via TextureCache) against the in-flight Epix Core2D depth prep; fixed the `ViewSortedRenderPhases` map access, implemented per-target depth pooling, and verified the Core2D query (using phase-containment as the `With<Camera2d>` equivalent). Rebuilt the no-modules `tests_header_render_render_parity_tests`, `tests_header_mesh_module_test`, and `examples_header_mesh_rendering`/`alpha_mask` via insider MSVC 14.52; ran the focused tests and captured each example's client area. | `render_parity_tests` (189) and `mesh_module_test` (5) passed. The mesh-rendering (1280x720, stable center `(174,195,189)`) and alpha-mask (960x540, stable center `(253,160,79)`, frames 0–1 identical hash `DF6FD7…`) examples render correctly, proving the moved Core2D depth prep plus `TextureCache` pooling works. C10 is marked ✅. \| **Separate pre-existing race:** a startup-only, intermittent wgpu `invalid command buffer` abort in `wgpuQueueSubmit` (reached from `RenderGraphRunner::run` → `queue.submit` at `graph_runner.cpp:25`) was observed on `examples_header_mesh_batching`. It is NOT caused by C10: a controlled 12-run loop panicked once on the untouched HEAD state (depth still in `ViewPlugin`) too, while C10 panicked at a similar low rate. This is an independent render-module race in the parallel command-buffer submission path and is tracked separately (see the render module tracker); it must not be attributed to C10. |
| 2026-09-03 | Startup low-FPS (`examples_header_mesh_batching`): temporary frame-time instrumentation (render-side `render_system` delta vs `work`, plus `extract_meshes_2d` and `prepare_mesh_instances` timings) across several runs. | Confirmed the several-frames low-FPS is a **progressively growing frame-to-frame interval** (e.g. one run: 12→43→54→77 ms and then spikes to 100–420 ms before recovering). It is **not** caused by `render_system` (its `work` was 0.5–8 ms), `extract_meshes_2d` (0.2–0.4 ms), or `prepare_mesh_instances` (0.03–0.55 ms), all constant. The Slang→SPIR-V compile runs on background pool threads (async), and the upscale `block_on_render_pipeline` adds only ~120 ms on frame 1 (removing it did not remove the multi-frame stall), so neither is the several-frames cause. The remaining suspects are the mesh `queue_meshes_2d_*` systems or a GPU-sync/swapchain/driver wait (graph submits asynchronously; a growing GPU backlog or lazy driver compilation would inflate the frame interval while per-phase CPU cost stays small). Diagnosing beyond this requires a severe-run frame log (user can reliably reproduce; this session could not). Instrumentation was temporary and reverted. |
| 2026-09-04 | Follow-up: timed `queue_meshes_2d_opaque` too; all four render-sub-app CPU phases now measured (`render_system` work, `extract_meshes_2d`, `prepare_mesh_instances`, `queue_meshes_2d_opaque`) and all are constant and tiny (0.03–8 ms). | Since every measured render-sub-app CPU phase is constant while the frame interval still grows, the growing cost is **outside the render sub-app**: either in the **main-world Update pipeline** (the render sub-app likely runs pipelined or in parallel with it, so its cost isn't visible in the render-side deltas) or in a **GPU/swapchain/driver wait** that the render-side CPU phase timing does not capture. Next step: measure the main-world Update/extract frame time (or use the user's severe-run `[renft]` frame log to correlate the growth with the presented-frame cadence). Temporary instrumentation reverted. |
| 2026-09-04 | Executor investigation: the render schedule (`RenderT`) runs on `AutoExecutor`. Temporary per-frame logging of the executor pick + the render frame delta across 6 runs per executor type (auto/taskflow/flat/classic/single). | The startup low-FPS correlates with **`AutoExecutor`**: the render schedule had intermittent large frame spikes (max 53–130 ms) under `auto`, while fixed executors were stable — `classic` ~7–10 ms, `single` ~3–9 ms, `flat` ~16–26 ms — with **no** 100 ms frame. Per the owner's logic, no single fixed executor reproduced the high cost, so the "auto picks a slow executor" mechanism is **not** fully confirmed; it is more likely the auto exploration/switching overhead. Separately, `TaskflowExecutor` **sometimes produces a white window** (render incomplete), indicating a taskflow scheduling/correctness bug. Both point to using a **fixed reliable executor** for the render schedule (`MultithreadClassicExecutor` is the recommended stable, parallel choice) rather than `auto` (startup spikes) or `taskflow` (white-window correctness bug). The taskflow white-window root cause is not yet isolated. |
| 2026-09-04 | Root-cause correlation: logged the render schedule's `AutoExecutor` pick per frame (executor index + duration) and correlated with the frame deltas. | **Refuted the "auto picks a slow executor" theory.** The **first render frame itself took 150 ms on `MultithreadClassicExecutor`** (a stable fixed executor), and all subsequent picks (taskflow/flat/single/classic) were 0.5–5 ms. So the ~150 ms first-frame cost is **first-frame initialization** (shader compile, buffer/pipeline setup) that occurs regardless of which executor runs it — it is **not** an executor/scheduling bug and is not a Bevy parity mismatch. No executor change was made (the earlier fix proposal and reasoning were wrong). The `TaskflowExecutor` white-window correctness bug remains a separate, un-isolated issue. |
| 2026-09-04 | **Confirmed cause:** removed `TaskflowExecutor` from `AutoExecutor`'s candidates and measured render frame deltas across 16 runs. | **The startup low-FPS is caused by the `TaskflowExecutor`.** With taskflow present, `auto` produced 53–130 ms frame spikes in ~half of runs; with taskflow excluded, **15/16 runs had a max frame delta of 3.9–7.5 ms** (1 outlier 60 ms) — no 100 ms+ spikes, and the batching example renders correctly (center `(42,42,46)`, no white window). The same taskflow scheduling bug underlies both the startup spikes and the intermittent white-window (incomplete frame). **Fix applied:** `TaskflowExecutor` is excluded from `AutoExecutor` candidates (committed `332e936a`), so it is no longer picked by auto while remaining available for explicit use. Tests: `render_parity_tests` 189 ✓, `mesh_module_test` 5 ✓. The underlying `TaskflowExecutor` scheduling/precedence bug remains open for a dedicated root-cause (tracked separately). |
| 2026-09-05 | M2/M3/M4/M5 swap: ported Bexy's `offset-allocator` v0.2.0 into `epix::mesh::offset_allocator`; reworked `MeshAllocator` to Bevy's model (offest-allocator-backed `GeneralSlab` with resident/pending, `Slab::LargeObject`, `slab_layouts`, Bevy's `allocations()` count, `allocate(id,bytes,layout)`, `free` reclaiming slabs); added `MeshVertexBufferLayout`/`Ref`/`Layouts` interning + `Mesh::get_mesh_vertex_buffer_layout`; added `RenderMesh` + `RenderMeshBufferInfo`; replaced `GPUMesh` with `RenderMesh` metadata as `RenderAsset<Mesh>::ProcessedAsset`; added `MeshAllocatorPlugin` + `allocate_and_free_meshes` (`PrepareAssets`, before `prepare_assets`); switched the mesh and text 2D draw commands to interleaved allocator-slice binding with layout-keyed pipelines. Rebuilt no-modules (MSVC 14.52): 27 mesh, 189 render parity, 1 text shape test pass. | The mesh module is now Bevy-0.18-shaped for M2/M3/M4/M5: metadata-only `RenderMesh`, shared allocator ownership of GPU buffers, offset-allocator reclamation, interleaved mesh pipelines keyed by interned layout, and allocator-slice draws. Client-area captures (PrintWindow): mesh_rendering 1280x720 center (174,195,189), frames 0–2 all SHA-256 `907F60D8E544F4F3CE3B246E184A5103434E616FBD24A7CC0CD5DDA9092DAC22` (identical to the pre-rework C10 baseline, scene intact: opaque square, both textured gradients, overlapping transparent circle/rectangle); alpha_mask 960x540 center (253,160,79), frames 0–1 SHA-256 `0818F3924F0CE60BE4807F5508EFDDC10C7BAB4E59FF322CF95E5DA528F989ED` (baseline); batching 1280x720 center (42,42,46); font_image 1280x720 center (42,42,46), frames 0–1 identical SHA-256 `D953F1E7BD18386A118AF804106127E805043DF699ED382CC36DB44AB32F25D0`. Accepted divergences: two-phase `SlabsToReallocate` is inlined into `ensure_slab_buffer`; `general_vertex_slabs_supported` always true (Vulkan-only); `extra_buffer_usages` not plumbed; settings live on the allocator resource rather than a separate `MeshAllocatorSettings` resource; morph (`M8`) out of scope. |






| 2026-09-06 | C16: ported evy_core_pipeline's tonemapping facility (	onemapping/mod.rs, 
ode.rs, 	onemapping.wgsl, 	onemapping_shared.wgsl, lut_bindings.wgsl, maths.wgsl powsafe, color_operations.wgsl hsv/rgb) into core_graph: TonemappingLuts (Handle<Image>, Bevy default-build placeholder), TonemappingPipeline/Key/flags, prepare_view_tonemapping_pipelines (per-With<ViewTarget> view, insert only ViewTonemappingPipeline), TonemappingNode (ViewUniformOffset/ViewTarget/ViewTonemappingPipeline/Tonemapping query + post_process_write + 5-entry bind group), TonemappingPlugin (ExtractComponentPlugin + ExtractResourcePlugin + RenderStartup init + Prepare system), and the Core2D Tonemapping node now a ViewNodeRunner{TonemappingNode}. Rebuilt no-modules (MSVC 14.52, -j10): 189 render parity + 27 mesh tests pass; examples_header_mesh_rendering runs with zero validation errors/panics and its capture center is the baseline (174,195,189). | C16 is a faithful port. Documented accepted divergences: (1) the real ktx2 LUTs are BasisLZ-compressed BC3/BC7 and need a KTX2/BasisLZ decoder Epix lacks, so Bevy's 	onemapping_luts-disabled default (magenta placeholder 3D LUT) is used — LUT methods behave exactly as Bevy without that feature; (2) Bevy extracts Tonemapping/DebandDither onto the single camera entity that also holds ViewTarget, whereas Epix's two-entity camera model places them on the synced camera entity and the render targets on a separately spawned view entity, so the node's required &Tonemapping query does not match and enabled tonemapping is not yet triggered — deliberately NOT bridged (the faithful ExtractComponentPlugin mechanism is used; the placement is a structural bevy divergence for the camera audit). The default Core2D None/non-HDR path is unchanged. |
| 2026-09-06 | C3: ported Bevy `fullscreen_material.rs` into core_graph (`FullscreenMaterial` concept, `FullscreenMaterialPlugin<T>`, `FullscreenMaterialNode<T>`, `FullscreenMaterialPipeline`, dedicated `FullscreenMaterialLabel`); fixed a latent render-module bug in `prepare_uniform_components` (it called `.get()` on the raw `const C&` query item, which only worked because the test probe coincidentally had a `.get()` member). Rebuilt no-modules (MSVC 14.52, -j10), ran the render suite (190 pass, incl. new `FullscreenMaterial.StructuredForBevyParity`), and captured `examples_header_render_glfw_fullscreen_material`. | C3 verified end-to-end: the example renders a full-frame dark-red tint (center `(38,13,13)` on the cleared frame, inspected visually), and the render suite is 190 pass (189 baseline + the new test). Accepted C++/Epix adaptations (encase `ShaderType`+`WriteInto` → `ShaderWritable`; `ShaderRef` → source-relative shader path loaded under `embedded://`; `FullscreenMaterialLabel` → a `NodeLabel` subtype; `extract_on_add` handles `Camera2d` only because no `Core3D` graph exists). A pre-existing tonemapping-shader `epix::view` import failure is observed on the untouched mesh_rendering example too; it is unrelated to C3 and only affects cameras using a non-`None` tonemapping method (the default camera's tonemapping node skips). |
| 2026-09-06 | M5: compared Bevy 0.18 `mesh/allocator.rs` line by line with Epix's allocator and plugin integration. Implemented Bevy's allocate-all → grow-once → upload-all frame flow, finish/ready-time resource creation, public system and `extra_buffer_usages`, actual-buffer accounting, capacity/ID edge semantics, and private implementation boundary. | Performed a full no-modules CMake reconfigure, clean of 1,349 outputs, and complete 994-action MSVC 14.52 rebuild at `--parallel 10`. Passed 27 mesh, 148 render, 9 core-graph, 3 embedded-shader, and 1 text-shaping tests. Three `PrintWindow(PW_CLIENTONLY)` captures of each actual process-owned GLFW window—mesh rendering, alpha mask, 3,300-item batching, and text font image—were visually inspected and remained non-black/correct; each example's scene-only crop had one identical SHA-256 across all three frames (`CAF3D728…`, `C96697E8…`, `0A32C707…`, `0421B45D…`). All eight stdout/stderr logs were free of error, fatal, validation, panic, and exception matches. |
| 2026-09-06 | M4: compared Bevy 0.18 `Mesh` construction, cloning, `asset_usage`, primitive factories, `take_gpu_data`, and `RenderAsset<RenderMesh>` usage with Epix. Added required per-mesh usage, deep-copy semantics, usage-preserving extraction, and explicit usage at every direct construction site. | Performed a full no-modules CMake reconfigure, clean of 1,349 outputs, and complete 994-action MSVC 14.52 rebuild at `--parallel 10`. Passed 29 mesh, 148 render, 9 core-graph, 3 embedded-shader, and 1 text-shaping tests (190 total). Three actual process-owned GLFW client captures each of mesh rendering, alpha mask, 3,300-item batching, and text font image were visually inspected and correct; every example's scene-only crop was byte-stable across all three frames (`77277819…`, `60D93113…`, `304EA7C8…`, `CD42D560…`). Capture logs had no error, fatal, validation, panic, or exception matches. M9 separately tracks Bevy's explicit extracted-data state and access errors. |
| 2026-09-06 | M9: compared Bevy 0.18 `MeshExtractableData`, `MeshAccessError`, mesh accessors, cloning, and `take_gpu_data` with Epix. Added explicit data/absent/extracted states, Bevy-shaped fallible and infallible access APIs, lazy attribute ranges, and exact one-shot extraction for genuinely empty meshes. | Reconfigured and clean-built the complete no-modules tree with MSVC 14.52 at `--parallel 10` (733 actions after the targeted clean). Passed 30 mesh, 148 render, 9 core-graph, 3 embedded-shader, and 1 text-shaping tests (191 total). Three actual process-owned GLFW client captures each of mesh rendering, alpha mask, 3,300-item batching, and text font image were visually inspected and correct; each example's scene-only crop had one identical SHA-256 across all three frames (`17163265…`, `A18A5F77…`, `03A71AE6…`, `30DA9E69…`). Capture logs had no error, fatal, validation, panic, or exception matches. M10 separately tracks built-in attribute IDs and shader-location remapping. |
| 2026-09-06 | Parent ECS checkpoint: upgraded `QueryIter` from a combined view/iterator into a non-owning C++ ranges view with independent input cursors and a lightweight sentinel. Archetypal filters now compile out per-entity filter work and expose exact `size()`; dynamic filters retain upper-bound-only `max_remaining()`. | Reconfigured and clean-built the full 994-action no-modules graph with MSVC 14.52 at `--parallel 10`. Passed 21 ECS/app/camera/mesh/text tests, 14 asset tests, and all 3 render tests. Added header/module range-concept and behavior coverage, including borrowed/input/non-common range shape, const-read-only iteration, view composition, exact archetypal sizing, and proof that archetypal `filter_fetch` is not called. Bevy's dense table-storage iteration remains a separate ECS-wide optimization and is not claimed by this checkpoint. |
| 2026-09-07 | C16/P5 tonemapping completion: matched Bevy's exact key/query/cache contracts, embedded its three real default LUT assets, added narrow KTX2/Zstandard decode, and implemented render-only `Image` payload transfer. Added a dedicated live 4×2 tonemapping comparison example whose title and logs map every algorithm to its region. | Reconfigured, cleaned 1,403 outputs, and completed the full 1,043-action no-modules MSVC 14.52 rebuild at `--parallel 10`; 3 image, 149 render, and 11 core-graph tests passed afterward. Four `PrintWindow(PW_CLIENTONLY)` captures targeted the actual 1280×720 GLFW render HWND. Every frame contained eight non-magenta, algorithm-distinct tiles, all four SHA-256 hashes differed with the continuously changing HDR input, and stdout repeated the exact top/bottom mapping with no stderr or validation errors. |
| 2026-09-08 | C16f visual-example extension: retained the clear-color `tonemapping` camera-component example and added the separate `tonemapping_hdr_images` direct-pipeline example. Its example-local Core2D graph node invokes all eight production `TonemappingPipeline` specializations in one 4×2 render pass. Added three 1K CC0 Poly Haven HDR environments as ordinary repository assets; the new example cycles only those images, leaving animated clear-color coverage to the original example. | Built the no-modules direct-pipeline example at `--parallel 10`. Direct `PrintWindow(PW_CLIENTONLY)` captures of the actual 1280×320 GLFW client covered Studio Small 01 → Lythwood Lounge → Venice Sunset → Studio Small 01. Every capture showed all eight regions, each HDRI appeared in every region without aspect distortion, titles changed with the active source while retaining the full algorithm map, logs contained the same mapping, and stdout/stderr had no validation or runtime errors. |
| 2026-09-08 | M1/M5/M7/M15 and S1/S6 ownership checkpoint: moved render mesh metadata/allocator to render, made camera own `MeshAabb<Mesh>`, and split CPU `epix_sprite` from higher `epix_sprite_render`; removed the temporary `epix_mesh_render` shape. | Reconfigured the no-modules build and rebuilt affected targets at `--parallel 10`. Mesh, camera, mesh-allocator, and render-parity suites all passed. Three `PrintWindow(PW_CLIENTONLY)` captures of each actual `GLFW30` window were taken for mesh rendering, alpha mask, 3,300-item batching, sprite basic, and sprite pressure. All five scenes were visually non-black/correct; every scene-cropped three-frame set recorded in `documentation/tmp/sprite-render-split-visual` was byte-identical within its example (`3C974AEA…`, `13511ABA…`, `9B177C70…`, `40D067F0…`, `64AC1F38…`), and all logs were free of error/fatal/validation/panic/exception matches. |
| 2026-09-09 | M14: compared Bevy 0.18 `bevy_mesh::components` and `MeshPlugin` directly with Epix. Added `Mesh3d`, `MeshTag`, both mesh-newtype defaults/dereference/conversions/required `Transform`, `AsAssetId<Mesh3d>`, and Bevy's modified-asset change-propagation system with its PostUpdate ordering. | Reconfigured, cleaned 1,478 outputs, and completed the full 1,093-action no-modules MSVC 14.52 rebuild at `--parallel 10`. The focused mesh executable passed 11 tests, including component/API and event-to-change-tick regressions; all 73 CTest executables passed sequentially. Three process-owned `GLFW30` client captures each of mesh rendering, 3,300-item batching, and alpha mask were visually inspected and correct. Scene-only crops were byte-identical across all three frames for each example (`C67CC00F…`, `13AD033A…`, `1B06D41C…`), and all stderr logs were empty. |
| 2026-09-10 | M17a: compared Bevy 0.18 `bevy_mesh::index::Indices` and `Mesh::get_index_buffer_bytes` with Epix. Replaced the type-erased public index representation with explicit U16/U32 alternatives, lazy iteration, Bevy names, width-preserving append/promotion, index-format conversion, and borrowed byte access; migrated all consumers. | Reconfigured, cleaned 1,478 outputs, and completed the full 1,093-action no-modules MSVC 14.52 rebuild at `--parallel 10`; all 73 CTest executables passed sequentially. Three process-owned `GLFW30` client captures each of mesh rendering, 3,300-item batching, alpha mask, falling sand, and interactive text were inspected. All scenes rendered correctly over every frame, live overlays advanced, all applications remained responsive, and all five stderr logs were empty. |
| 2026-09-10 | M17b: compared Bevy 0.18 `MeshWindingInvertError` and `MeshTrianglesError` directly with Epix. Added tagged C++ variants for every Bevy alternative, nested `MeshAccessError` propagation, and exact display text. | The no-modules mesh target rebuilt at `--parallel 10`; its complete test executable passed, including focused alternative and message checks in both header and module test sources. This is a value/API-only mesh change with no rendering behavior, so no visual example or capture is applicable. |
