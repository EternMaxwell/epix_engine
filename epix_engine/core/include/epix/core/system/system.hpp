#pragma once

#include "param.hpp"

namespace epix::core::system {
template <typename In, typename Out>
struct SystemInterface {
    
};
template <typename In, typename Out>
struct SystemUnique {};
}  // namespace epix::core::system